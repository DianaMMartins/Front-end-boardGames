//displays the category and what reviews it has
export const CategoryPage = ({ eachCategory, title }) => {
  console.log(eachCategory, title);

  return (
    <section className="category-page">
      This category and it's reviews!
      {/* select reviews by category_id */}
    </section>
  );
};
